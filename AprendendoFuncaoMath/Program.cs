using System;
namespace AprendendoFuncaoMath
{
    class Program
    {
        static void Main()
        {
            // Sqrt funçao da raiz quadrada, so aceita numeros do tipo double
            Console.WriteLine(Math.Sqrt(16.0)); // Raiz Quadrada
            Console.WriteLine(Math.Pow(2,10)); // Potencias
            Console.ReadKey();
        }
    }
}
